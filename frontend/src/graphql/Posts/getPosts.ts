import { DocumentNode, gql } from '@apollo/client';

export const GET_POSTS: DocumentNode = gql`
  {
    getPosts {
      id
      body
      createdAt
      username
      likeCount
      likes {
        username
      }
      commentCount
      comments {
        id
        username
        createdAt
        body
      }
    }
  }
`;
